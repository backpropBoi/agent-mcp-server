{{- $root := . -}}
{{- if .Values.configMaps }}
{{- range .Values.configMaps }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
data:
  {{ .subPath }}: |
    {{- ($root.Files.Get .file) | nindent 4 }}
---
{{- end }}
{{- end }}